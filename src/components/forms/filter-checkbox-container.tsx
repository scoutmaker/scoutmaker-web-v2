import { styled } from '@mui/material'

export const FilterCheckboxContainer = styled('div')(({ theme }) => ({
  display: 'flex',
  justifyContent: 'center',
  marginTop: theme.spacing(-1),
  marginBottom: theme.spacing(1),
}))
